class Helper {
    static rootPath = '';
    static publicPath = '';

    static getRootPath() {
        return Helper.rootPath;
    }

    static getPublicPath() {
        return Helper.publicPath;
    }
}

module.exports = Helper;